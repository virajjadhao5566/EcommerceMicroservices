server:
  port:8080

spring:
  h2:
    console:
      enabled: true
    datasource:
      url:jdbc:h2:file:~/data/product_db
      driverClassName:org.h2.Driver
      username:viraj
      password:viraj
  jpa:
    database-platform:
      org:
        hibernate:
          dialect:H2Dialect
    hibernate:
      ddl-auto:update